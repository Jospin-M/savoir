export function submitForm(form: any) { // this can be a wrapper that returns the appropriate handler function
    return ((event: React.MouseEvent<HTMLButtonElement, MouseEvent>) => {
        // this is where you can retrieve form input
    });
}